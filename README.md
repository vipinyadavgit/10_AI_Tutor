# 10_AI_Tutor

## The main objective of creating this solution is to understand the roles:

    System role: Top level role, which decides the behavior of the LLM
    User role: That takes the input from the user and passes to the LLM
    Assistant role: Provides the output of the LLM
